services:
  - type: web
    name: mbappeking
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: node api/server.js
    envVars:
      - key: NODE_ENV
        value: production